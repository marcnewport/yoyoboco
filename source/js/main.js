var utils = require('./utils');
var home = require('./home');

utils();

(function($) {

  global.$document = $(document);

  $document.ready(function() {

    'use strict';

    // home();

  });

}(jQuery));
